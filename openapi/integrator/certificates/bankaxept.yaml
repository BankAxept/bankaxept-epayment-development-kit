openapi: 3.0.0
info:
  version: 0.1.0
  title: BankAxept ePayment Certificates API - ePayment Platform
  description: BankAxept ePayment Certificates API
servers:
  - url: https://api.epp-stoetest.cloud/certificates
paths:
  /wallet:
    get:
      operationId: walletCertificates
      responses:
        "200":
          description: OK
          content:
            '*/*':
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CertificateData"
  /merchant:
    get:
      operationId: merchantCertificates
      responses:
        "200":
          description: OK
          content:
            '*/*':
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CertificateData"
components:
  schemas:
    CertificateData:
      type: object
      required:
        - name
        - subject
        - notBefore
        - expiresOn
        - certificate
      properties:
        name:
          type: string
        subject:
          type: string
        notBefore:
          type: string
          format: date-time
        expiresOn:
          type: string
          format: date-time
        certificate:
          type: string
